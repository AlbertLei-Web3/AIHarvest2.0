{
  "name": "aiharvest-subgraph",
  "version": "1.0.0",
  "description": "The Graph subgraph for AIHarvest platform",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/",
    "create-local": "graph create --node http://localhost:8020/ aiharvest",
    "remove-local": "graph remove --node http://localhost:8020/ aiharvest",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 aiharvest"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.55.0",
    "@graphprotocol/graph-ts": "0.31.0"
  }
} 